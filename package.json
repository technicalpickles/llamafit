{
  "name": "llamafit",
  "version": "0.2.0",
  "description": "Which local LLMs actually fit this machine? Checks models against real memory headroom.",
  "license": "MIT",
  "author": "Josh Nichols",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/technicalpickles/llamafit"
  },
  "bin": {
    "llamafit": "./dist/cli.js"
  },
  "files": [
    "dist",
    "data",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm test && npm run build"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.23.9",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
